<?xml version="1.0" encoding="UTF-8"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0">
    <feature name='cbs' version='${project.version}'>
        <feature>cxf</feature>
        <bundle>mvn:${project.groupId}/core/${project.version}
        </bundle>
        <bundle>mvn:${project.groupId}/dao/${project.version}
        </bundle>
        <bundle>mvn:${project.groupId}/commands/${project.version}
        </bundle>
    </feature>
</features>
